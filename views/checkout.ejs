<head>
	<%- include('includes/head.ejs') %>
	<link rel="stylesheet" type="text/css" href="/css/checkout.css">
	<script>
	function Bienvenida(){
		
		swal({
  title: "RECORDATORIO",
  text: "Esta es una pagina demostrativa. No ponga datos sensibles porfavor. En caso de querer probar el sistema de pagos porfavor use la tarjeta de prueba 49494949494949494949. No aceptara otros numeros para evitar accidentes. Gracias",
  icon: "info",
  button: "ยกEntendido!",
	}).then((value) => {
  //location.reload();
});
		
	}
	</script>
</head>
<body onLoad="Bienvenida()">
	
	<%- include('includes/navbarH2F.ejs') %>
	<ul class="slideshow">
      <li></li>
      <li></li>
      <li></li>
      <li></li>
    </ul>

	<div class="row espacing" class="container">
	  <div class="col-75">
		<div class="container">
		  <form action="/Checkout" method="POST" id="forma">
			<div class="row">
			  <div class="col-50">
				<h3>Sus datos</h3>
				<label for="fname">Nombre Completo</label>
				<input type="text" id="fname" name="nombreComp" placeholder="Juan Paco Pedro de la Mar" value="">
				<label for="email">Correo electronico</label>
				<input type="text" id="email" name="email" placeholder="john@ejemplo.com" value="">
				<label for="adr"><i class="fa fa-address-card-o"></i>Direccion</label>
				<input type="text" id="adr" name="direccion" placeholder="542 W. 15th Street" value="">
				<label for="city"><i class="fa fa-institution"></i>Ciudad</label>
				<input type="text" id="city" name="ciudad" placeholder="New York" value="">
				<div class="row">
				  <div class="col-50">
					<label for="state">Estado</label>
					<input type="text" id="state" name="estado" placeholder="NY" value="">
				  </div>
				  <div class="col-50">
					<label for="zip">Codigo Postal o ZIP</label>
					<input type="text" id="zip" name="CP" placeholder="10001"  maxlength="5" value="">
				  </div>
				</div>
			  </div>
			</div>
		  </form>
		</div>
	  </div>
	</div>

<script>
	function recargaForzada(){
		var f = document.createElement("FORM");
		f.style.display = "none";
		document.body.appendChild(f);
		f.action = "/H2Flow/Checkout";
		f.method = "POST";
		var x = document.createElement("INPUT");
		x.setAttribute("type", "text");
		var arrayObjetos = [];
		for(var i=0;i<=sessionStorage.getItem("numObjetos");i++){	//si tienes problemas con la cantidad de objetos cambia el .lenght por sessionStorage.getItem("numObjetos")
			arrayObjetos.push(sessionStorage.getItem("producto" + i));
		}
		x.value = arrayObjetos;
		x.name = "arrayProductos";
		var y = document.createElement("INPUT");
		y.setAttribute("type", "text");
		y.value = sessionStorage.getItem("numObjetos"); 
		y.name = "contador";
		f.appendChild(y);
		f.appendChild(x);
		f.submit();
	}
</script>
	<div class="row espacing" class="container">
		<%- include('includes/checkaoutpago.ejs') %>
	</div>
</body>